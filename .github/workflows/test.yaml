name: 'Test Workflow'

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Echo
        run: echo 'hello world'
